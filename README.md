# api-rest tutorial made with Node.js, Express and MongoDB

## Instalation
1. ```npm install```
2. After installing mongodb on your platform, run ```mongod``` on a new terminal
3. ```npm start``` to run the node server